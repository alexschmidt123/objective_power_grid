# Dependencies for conda env ``mocu_optimized`` (Python 3.10).
#
# Install the GPU-specific PyTorch and CUDA toolkit first, then install this
# file.  The RTX 4090 setup is the primary/reference environment.
#
# RTX 4090 (primary/reference; CUDA 12.1):
#   conda create -n mocu_optimized python=3.10 -y
#   conda activate mocu_optimized
#   pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 \
#     --index-url https://download.pytorch.org/whl/cu121
#   conda install -c "nvidia/label/cuda-12.1.1" cuda-toolkit -y
#
# RTX 5080 (alternative; CUDA 12.8 / Blackwell) -- keep this path commented:
#   conda create -n mocu_optimized python=3.10 -y
#   conda activate mocu_optimized
#   pip install torch torchvision torchaudio \
#     --index-url https://download.pytorch.org/whl/cu128
#   conda install -c nvidia cuda-toolkit=12.8 gxx_linux-64 -y
#
# Then:
#   pip install -r requirements.txt
#   export PYTHONPATH="$(pwd)"

# --- core (used by src/) ---
numpy==2.2.6
scipy==1.15.2
PyYAML==6.0.3
matplotlib==3.10.7
tqdm==4.67.1

# PyTorch must be installed from the GPU-specific index above before this file.
# The lower bound accepts both the reference 2.4.0+cu121 build (RTX 4090) and
# the newer cu128 build required by the RTX 5080. Do not install CPU-only torch.
torch>=2.4.0

# CUDA observation / bank generation
pycuda==2025.1.2
Mako==1.3.10
pytools==2025.2.5
platformdirs==4.5.0
siphash24==1.8
MarkupSafe==2.1.5

# Shared torch runtime pins (validated with both cu121 and cu128 environments)
filelock==3.19.1
fsspec==2025.9.0
Jinja2==3.1.6
networkx==3.3
sympy==1.14.0
typing_extensions==4.15.0
pillow==11.3.0
